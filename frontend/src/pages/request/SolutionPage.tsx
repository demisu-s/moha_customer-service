import RequestSolutionComponent from "../../components/requestComponent/RequestSolutionComponent";

const SolutionPage: React.FC = () => {
  
  return (
    
    <RequestSolutionComponent/>
  );
};

export default SolutionPage;
